spring:
  application:
    name: limits-service
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: dev
  cloud:
    config:
      profile: dev
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
limits-service:
  minimum: 2
  maximum: 999
